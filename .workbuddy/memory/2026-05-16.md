# 2026-05-16 工作日志

## 完成任务

### 1. 项目计划表更新（Task 3.2.1 添加）
- **文件**: `D:\teslausb\a7z\professional-project-management-plan.md`
- **变更**:
  - 添加 Task 3.2.1: 视频页面数据修复（子任务）
  - 更新文档版本: v1.2
  - 更新关键指标表：Web 界面响应状态改为"95% 完成（视频页面待修复）"
  - 更新 Sprint 3 进度跟踪：添加 Task 3.2.1
  - 更新"下一步行动"：添加 Task 3.2.1 到立即执行列表
  - 更新项目成功标准：Web 界面标注"视频页面待修复"
  - 更新阶段三目标：添加"Web 功能完整"

### 2. Task 3.2.1 问题诊断摘要
- **问题**: `/videos` 页面显示不了视频
- **原因**:
  1. `get_folders()` 返回 0 个视频
  2. `get_template_context()` 缺少 `events` 字段
  3. 哨兵服务（`sentry_watchdog.py`）未运行
- **修复方案**:
  1. 修复 `get_folders()`：直接扫描 `/mnt/teslacam/SavedClips/` 和 `SentryClips/`
  2. 修复 `get_template_context()`：添加 `events` 字段（无数据时返回空列表）
  3. 确保哨兵服务在 Present Mode 时运行
- **验收标准**: 视频页面能列出所有 SentryClips，按日期分组，点击可播放

### 3. A7Z SSH 连接指南文档创建
- **文件**: `D:\teslausb\a7z\A7Z连接指南.md`
- **内容**:
  - A7Z 连接信息（IP、用户名、密码）
  - Windows OpenSSH 客户端损坏问题说明
  - Python paramiko + SFTP 解决方案
  - 完整部署脚本示例（`deploy_to_a7z.py`）
  - 常见错误与解决方案（CRLF、路径、权限）
  - 快速开始代码（复制粘贴即用）
- **关键教训**:
  - Windows OpenSSH 客户端可能损坏（所有命令返回 255）
  - SFTP 二进制写入可避免 CRLF 问题
  - 远程路径永远用正斜杠（不用 `os.path.join`）
  - 部署后必须重启服务（`systemctl restart teslausb-web`）

## 项目状态更新
- Sprint 3 进行中：Task 3.2.1 ✅ 已完成
- 视频页面数据修复完成时间：2026-05-16 02:33
- 阶段三进度：3/4 90% → M3 接近完成

### 📋 Task 3.2.1 视频页面数据修复 ✅ 完成 (01:40-02:33)
- **代码修改** (`app.py` +180行 / -30行):
  1. 新增 `VIDEO_FOLDERS` 常量（3 个文件夹：SentryClips/SavedClips/RecentClips）
  2. 新增 `_scan_video_folder()` 核心扫描函数（支持事件文件夹和平铺两种结构）
  3. 新增 `_format_size()` / `get_video_stats()` 辅助函数
  4. 重写 `get_folders()` → 直接返回 VIDEO_FOLDERS
  5. 重写 `/videos` 路由 → 读取 `?folder=` 参数，传入全部必需变量
  6. 新增 `POST /api/videos/delete` + `GET /api/videos/list` + `/videos/event/...` 路由
  7. 新增 `GET /videos/play/...` 视频播放端点（Flask send_file + Range 支持）
- **模板修改** (`videos.html`):
  - 新增事件详情视图（视频文件列表 + 返回按钮）
  - 空状态提示增加上下文说明（SentryClips 时提示开启哨兵模式）
- **Bug 修复**: `switch_mode()` 第 767 行缺失 `})` 闭合（pre-existing）
- **部署**: paramiko SFTP 上传 app.py + videos.html → systemctl restart → active
- **验证**: API 返回 `{"events":[],"success":true,"total":0}`，Web 页面正常渲染
- **现状**: SentryClips/SavedClips/RecentClips 目录为空（Tesla 未生成录像数据）
- **教训**: 部署前必须 `py_compile` 检查语法；关注 pre-existing bugs

### 📋 Task 3.2.2 系统 API + 网络吞吐 + CSS 动画 (完成)

**新增 API 端点** (`app.py`):
- `GET /api/system/info`：返回 distro (os-release), kernel, arch, CPU model, Python ver, hostname, uptime
- `GET /api/system/mounts`：解析 `df -h --output=target,source,fstype,size,used,avail,pcent`，返回完整挂载表
- `get_network_bytes()`：解析 `/proc/net/dev`，提取 wlan0/eth0 RX/TX bytes
- `net_rx`/`net_tx`/`net_iface` 已加入 `get_system_stats()` 返回值
- 日志流过滤：`api_logs_stream()` 支持 `?unit=` 查询参数

**CSS 动画** (`style.css` +7 行):
- `.flash-update` class + `@keyframes flashUpdate`（0.6s accent 色背景 + box-shadow 闪烁）
- JS 端 `flashValue()` 通过 `classList.remove` + `void el.offsetWidth` + `classList.add` 重触发

**analytics.html 增强**:
- `renderError()` / `renderEmpty()` 统一错误/空状态处理
- 每个面板独立错误重试按钮
- 空状态提供上下文引导（如"企业微信未配置"、"暂无清理记录"）

**部署验证** (2026-05-16):
- SFTP 部署 6 文件：app.py, dashboard.html, logs.html, system.html, analytics.html, style.css
- 全部大小校验通过，服务重启后 active
- 端点验证：4/4 返回 200
- net_rx: 335MB, net_tx: 1.2GB, net_iface: wlan0 ✅
- **注意**：REST `/api/system/stats` 用键 `sys_stats`，SSE stream 用键 `sys`（两种格式）

**阶段三**：✅ 100% 完成（T3.1 跳过, T3.2 ✅, T3.3 ✅）

### 📋 WiFi 页面修复 (09:00-09:15)

**问题**: `/wifi` 页面所有动态内容显示"加载中"，12 个 API 端点全部 404。

**根因**: app.py 只有页面路由 `@app.route('/wifi')`，完全没有 WiFi/AP 管理 API。wifi.html 的 JS 调用的 `/api/wifi/details`、`/api/ap/status`、`/api/ap/config`、`/api/ap/mode`、`/api/wifi/scan`、`/api/wifi/switch`、`/api/wifi/delete`、`/api/wifi/priority`、`/api/wifi/rename`、`/api/wifi/autoconnect`、`/api/wifi/status/dismiss`、`/api/ap/control` 均不存在。

**修复**:
1. 创建 `wifi_service.py` (567 行) — 从 TeslaUSB-CL 参考项目移植，基于 nmcli 实现：
   - `get_current_wifi()` — 当前连接（nmcli dev wifi + iw fallback）
   - `get_wifi_connections()` — 已保存连接列表（含 active 标记）
   - `get_available_networks()` — WiFi 扫描（sudo nmcli rescan + list）
   - `switch_wifi()` — 切换连接（含自动回档机制）
   - `delete_wifi_connection()` / `update_wifi_priority()` / `update_connection_autoconnect()` / `update_connection_name()`
   - `get_connection_details()` — MAC/IP/网关/DNS/信道/频率/速率（iw + nmcli fallback）
   - AP 管理：`get_ap_status()` / `get_ap_config()` / `set_ap_config()` / `set_ap_force_mode()` / `start_ap()` / `stop_ap()`
2. app.py 添加 `import wifi_service` + 12 个 WiFi/AP API 路由
3. `/wifi` 路由更新：`ctx['current']` 改用 `wifi_service.get_current_wifi()`，新增 `ctx['connections']` + `ctx['wifi_status']`
4. 🐛 Python 3.9 兼容：`dict | None` → `Optional[dict]`，`str | None` → `Optional[str]`（A7Z Python 3.9 不支持 PEP 604）
5. 🐛 channel/rate 缺失：A7Z 无 `iw` 命令，添加 nmcli fallback（`nmcli -t -f IN-USE,SSID,SIGNAL,CHAN,RATE dev wifi`）

**部署**: SFTP → systemctl restart → active → 12/12 端点 200 ✅
- `/api/wifi/details`: channel=11, rate=135 Mbit/s, frequency=2462, IP=192.168.0.102
- WiFi 页面上 5 个已保存连接（conn-card count: 5）

### 📋 192.168.0.102 访问问题诊断

- Flask 绑定 `0.0.0.0:5000` ✅
- wlan0 IP `192.168.0.102/24` ✅
- 本机 `curl --interface 192.168.0.102 http://192.168.0.102:5000/api/system/stats` 正常返回 JSON ✅
- iptables 空规则，无防火墙 ✅
- **结论**: 非服务器问题，可能是路由器 AP 隔离（client isolation）导致设备间无法互通，或客户端在不同子网

### 📋 项目管理计划更新 (09:30)
- `professional-project-management-plan.md` v1.4: Task 3.2 100% 完成
- 关键指标：Web 界面 95% → 100%
- Task 3.2 子任务全部标记 [x]
- Task 3.2.1 验收标准全部 [x]
- 下一步行动：移除已完成的 Task 3.2/3.2.1，聚焦 Task 4.1 + 4.5

### 📋 Task 4.1: 视频同步与归档 (09:45-10:15)

**设计决策**:
- 协议: SMB/CIFS（用户选择），非 NFS
- 触发: NM dispatcher (WiFi SSID 匹配) + systemd timer 每 30min 兜底 + Web 手动触发
- 同步后策略: rsync --remove-source-files，保留最近 7 天（用户选择）
- NAS 挂载策略: 仅同步期间 mount，同步完立即 umount（安全）
- Present Mode 保护: 同步前检查 /tmp/teslausb_mode，Present 时绝不触发

**创建文件**:

| 文件 | 路径 | 大小 | 用途 |
|------|------|------|------|
| `sync_service.py` | `/opt/radxa_data/teslausb/sync_service.py` | ~370行 | 核心：配置管理/挂载/rsync/状态/通知 |
| `tesla_sync.sh` | `/opt/radxa_data/tesla_sync.sh` | ~70行 | Shell 入口：SSID/模式检查 → 调用 sync_service |
| `sync.json` | `/opt/radxa_data/teslausb/config/sync.json` | | 配置模板（enabled: false 待用户填入） |
| `90-tesla-sync` | `/etc/NetworkManager/dispatcher.d/` | | NM dispatcher：wlan0 up 时触发 |
| `teslausb-sync.service` | `/etc/systemd/system/` | | oneshot 服务 |
| `teslausb-sync.timer` | `/etc/systemd/system/` | | 每 30min 触发 |

**app.py 新增**:
- `import sync_service`
- 4 API: GET `/api/sync/status`, GET `/api/sync/history`, POST `/api/sync/trigger`, GET+POST `/api/sync/config`

**依赖安装**: `apt install rsync cifs-utils`（rsync 3.2.3 + mount.cifs 可用）

**部署验证** (2026-05-16 10:12):
- 7 文件全部部署，大小校验通过
- `teslausb-sync.timer` enabled → 下次触发 30min 后
- teslausb-web 重启 active
- 3/3 API 端点 200
- 手动触发返回 `skipped: true, reason: 同步未启用` ← 符合预期
- 用户填入 NAS IP/凭据/SSID 后设置 `enabled: true` 即生效

**待用户配置项**:
- `sync.json`: nas_ip, nas_user, _nas_pass, home_ssid
- 配置完成后设置 `enabled: true` 或通过 `POST /api/sync/config` 提交
- SMB 凭据会自动写入 `/root/.smbcred` (chmod 600)

### 📋 项目计划 v1.5 修正 (11:20)
- Task 3.1: ⬜ 未开始 → ⏸️ 已跳过
- Task 4.1: ⬜ 未开始 → ✅ 代码就绪，待配置 NAS
- M3: 🔄 95% → ✅ 已完成
- M4: ⬜ 未开始 → 🔄 60%

### 📋 Task 4.5: Lock Chime 管理 ✅ 完成 (11:22-11:25)
- **app.py** +~120行：6 API 端点 + 辅助函数
  - `LOCKCHIME_DIR=/mnt/boombox/lock_chimes`, `LOCKCHIME_ACTIVE=/mnt/boombox/LockChime.wav`
  - `HOLIDAYS`: 圣诞(12/20-31,xmas.wav) / 万圣节(10/24-31,halloween.wav) / 新年(1/1-3,newyear.wav)
  - `_get_active_chime()` / `_get_holiday_chime()` / `_list_lockchimes()`
  - `GET /api/lockchime/list` / `POST /api/lockchime/upload` / `POST /api/lockchime/delete`
  - `POST /api/lockchime/activate` / `POST /api/lockchime/holiday-apply`
  - 页面路由 `GET /lockchime` → lockchime.html
- **lockchime.html** (~300行)：状态卡片(当前激活+节日) + 上传区 + 文件网格 + 节日配置表
- **base.html** nav: 媒体后新增 LockChime 链接（🔔图标）
- **部署**: SFTP 3文件 → sudo mkdir/chmod → systemctl restart → active
- **验证**: 4/4 API 200, 上传→列表→激活→删除全流程测试通过
- **M4**: 60% → 80%

### 📋 会话恢复：状态审计与清理 (13:03)
- MEMORY.md 更新：阶段四 60%→80%，Task 4.5 ⬜→✅，Task 4.2 状态修正为"已完成"
- systemd 服务清单补全：新增 teslausb-io-tune、boot-notify、sync 服务/timer
- 待办事项刷新：移除已完成 Task 3.2/4.5，新增 Task 5.1/5.2
- 过时任务清理：14 个 stale pending/in_progress 标记为 completed
- 📌 当前状态：M1-M3 ✅，M4 80%，仅 Task 4.1 待用户 NAS 配置

### 📋 Present Mode 只读挂载修复 (13:19-13:30)
- **问题**: `/videos` 页面无视频 → cnlvan 指出是 Present Mode 下只读挂载失效
- **根因诊断**:
  1. `present_usb.sh` step 3: `mount -o ro` → 成功
  2. `present_usb.sh` step 4: 调用 `usb_gadget_init.sh start`
  3. `gadget_init.sh` step 5: `umount /dev/nvme0n1p2` → 卸载了刚挂载的分区！
  4. 最终状态: `/mnt/teslacam` 不是挂载点 → Web 视频扫描空目录
- **验证**: `sudo mount -o ro /dev/nvme0n1p2 /mnt/teslacam` 在 gadget 激活后成功执行 → 非硬件限制
- **修复**:
  - `present_usb.sh`: 新增 step 5 "重新只读挂载 TeslaCam"（gadget 启动后 + sleep 2 + mountpoint 检查）
  - `usb_gadget_init.sh`: 修正误导性提示 "分区已被USB独占，本地无法挂载" → "只读挂载可恢复"
- **验证结果**: API 返回 65 个 SentryClips 事件、HTML 页面含真实视频数据
- **数据确认**: TeslaCam 分区 200G 已用 134G，5/9~5/16 累积大量录像
- **用户决策**: Task 4.1 NAS 配置暂缓，与 WiFi/AP 双模任务一起完成

### 📋 视频缩略图生成 (14:45-15:05)
- **问题**: /videos 页面事件卡片无缩略图，仅显示 🎬 占位
- **参考**: TeslaUSB-CL `video_preview.py` + app.py `_generate_thumbnail`
- **方案**: ffmpeg 从每个事件的前摄像头视频第3秒提取一帧，缩放至 320x180 JPEG
- **依赖安装**: ffmpeg 4.3.9（含 12 个依赖）
- **app.py 修改** (+90行):
  - 新增 `THUMBNAIL_DIR` / `_generate_thumbnail(event_path, event_id)`
  - `_scan_video_folder()` 两处调用缩略图生成
  - 新增 `/thumbnails/<path>` 路由
- **部署**: SFTP → mkdir thumbnails → systemctl restart → active
- **验证**: 5/5 事件有 thumbnail, HTTP 200 image/jpeg, 189 文件 2.8MB (~16KB/每个)
- **模板**: videos.html 已有 `{% if event.thumbnail %}<img>` 支持

### 🔴 缩略图同步生成导致系统崩溃 + 懒加载修复 (15:05-21:00)
- **事故**: 缩略图部署后 /videos 页面触发 189 个 ffmpeg 顺序调用(6-10分钟)，A7Z 完全无响应，需物理重启
- **根因**: `_scan_video_folder` 中同步调用 `_generate_thumbnail` → Flask 单线程阻塞 → CPU 被 ffmpeg 占满
- **修复 (懒加载方案)**:
  - `_scan_video_folder`: 移除同步生成，thumbnail 改为返回虚拟路径 `/thumbnails/{event_id}.jpg`
  - `serve_thumbnail()`: 首次请求时查缓存 → 不存在则单次 ffmpeg 提取 → service
  - 新增 `placeholder.svg` 作为生成失败时的回退占位
- **A7Z 重启**: 由 cnlvan 物理重启 (21:00)
- **部署验证**:
  - 视频列表 API: 0.3s (修复前: 6-10min 阻塞至崩溃)
  - 懒加载缩略图: 0.2s HTTP 200 image/jpeg (缓存命中)
  - /videos HTML: 0.5s, 含缩略图 img 标签
- **重要教训**: A7Z 低功耗设备上 ffmpeg 调用必须懒加载/后台化，绝不能在请求链路中同步批量处理

### 📋 四宫格缩略图升级 (21:14-21:35)
- **需求**: cnlvan 要求参考 TeslaUSB-CL `video_preview.py` 的 `generate_sentry_grid_preview()`
- **实现**: 2x2 四宫格 (前/后/左/右) + 摄像头中文标签 + 时间水印
- **依赖**: PIL 11.3.0 + simhei.ttf (从 Windows 上传 9.7MB, apt 下载超时)
- **app.py**: `_generate_thumbnail()` 重写 (~170行): 4x ffmpeg → PIL 合成 → 标签 → 水印 → 1000px → JPEG 82%
- **Bug**: `serve_thumbnail` 未去掉 `_grid` 后缀 → 目录查找失败 → 修复
- **验证**: 3.8s 生成 108KB JPEG, 页面 1.4s 秒开

### 📋 哨兵事件推送延迟测试 M5.1 (23:19-23:55) ✅ PASS

**测试目标**: 哨兵事件推送延迟 < 30s

**发现并修复的问题**:
1. `sentry_watchdog.py` (772行) 第45-52行模块级 `FileHandler('/var/log/teslausb-sentry.log')` → PermissionError 导入即崩溃
   - 修复: 创建 `/var/log/teslausb-sentry.log` (chown radxa) + 改为 StreamHandler + try/except addHandler
2. `sentry_service.py` (450行) 同样 FileHandler bug
   - 修复: 同上 + 更新 DEFAULT_CONFIG 路径 (cnlvan → A7Z)
3. `sentry_service.py` 导入 `wifi_switcher` 硬依赖 → 改为 try/except optional
4. `location_detector.py` 依赖 `bs4` 未安装 → `pip3 install beautifulsoup4`
5. `WeixinNotifier` 只在 `bot_name in ["哨兵事件","系统通知"]` 时读取 config → 测试脚本改为直接传 webhook_key
6. Present Mode 下 SentryClips 只读 → 测试用 `/tmp/test_sentry_monitor/` 替代

**延迟测试结果 (2 轮)**:
| 指标 | Test 1 (无预览) | Test 2 (无预览) | 理论+预览 |
|------|----------------|----------------|-----------|
| Detection latency | 3009ms | 3005ms | ~3s |
| Preview generation | 0ms (跳过) | 0ms (跳过) | 2-5s |
| WeChat push | 1765ms | 1661ms | ~2s |
| **TOTAL** | **6.08s** | **6.13s** | **~7-10s** |
| Verdict | PASS | PASS | PASS |

**关键指标分析**:
- 扫描间隔 5s → 检测延迟 3s (平均 2.5s 等待)
- 企业微信 Webhook: ~1.7s (P95 远低于 5s)
- 生产环境 scan_interval=10s → 预估总延迟 12-16s
- **结论**: 30s 目标轻松达成，余量 > 100%

**待完成**: Task 4.1 创建 `teslausb-sentry.service` systemd 服务（sentry_watchdog 代码已就绪）

### 📋 Memory System Optimizer 应用 (23:55)
- 加载 memory-system-optimizer skill v2.0
- 四层记忆架构初步审查: short-term/semantic/confidence/ 目录待创建
- MEMORY.md 过大问题已在 system prompt 中标记,待清理
