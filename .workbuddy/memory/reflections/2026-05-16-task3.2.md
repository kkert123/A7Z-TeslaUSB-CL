# 自省 — Task 3.2 收尾 + WiFi 修复 (2026-05-16)

## 置信度评估: 90%
- 所有端点已验证 200，数据结构匹配前端 JS
- 192.168.0.102 问题非服务器侧，路由器 AP 隔离判断准确

## 做对了什么
1. 诊断先行：先查 API 返回（404），再查代码（缺少路由），精准定位
2. 参考项目复用：从 TeslaUSB-CL 移植 wifi_service.py，节省大量开发时间
3. 分层部署：先推 wifi_service.py 测试 import，通过后再推 app.py
4. SFTP 部署流程成熟：大小校验 + restart + curl 验证三件套

## 教训
1. **Python 版本兼容**：A7Z Python 3.9 不支持 `dict | None`（PEP 604），应提前 `python3 --version` 确认
2. **系统工具可用性**：`iw` 在 A7Z 上不存在，nmcli fallback 是必须的
3. **模板变量遗漏**：wifi.html 需要 connections/wifi_status，但 get_template_context() 未提供 — 应读完模板再写路由

## 下次改进
- 部署前增加 `python3 -c "import <module>"` 预检步骤
- 新增 API 端点时同时检查前端模板的 fetch 调用列表
